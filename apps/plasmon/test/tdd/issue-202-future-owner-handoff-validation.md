# #202 future-owner validation handoff

Prerequisites: authorized runtime implementor; fresh installed package; current
#187 strict health helper; legal explicit js-dos fixture. RED files:
`issue-202-storage-bootstrap-red-spec.md` and the #187 packaged smoke allowance
entries. Preserve FS -> AssociationRegistry -> OpenService -> JsDosPlayer,
local assets, logical Program Files authority, sandbox/CSP, real canvas/player.
Change only js-dos storage bootstrap compatibility. Unspecified: exact adapter
API and vendor integration technique.

GREEN destination: focused runtime capability tests plus the installed js-dos
browser proof and a strict #187 run with both #202 allowances removed. Stop on
Ready, a documented external browser capability blocker, or a real security
boundary conflict; never weaken isolation to obtain green.
