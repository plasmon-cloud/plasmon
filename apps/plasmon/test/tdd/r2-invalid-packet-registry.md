# r2 invalid / superseded packet registry

These artifacts must not be consumed without the replacement noted.

| Issue | invalid artifact/class | why invalid | replacement / consume |
|---|---|---|---|
| #66 | historical fake stacking gate | asserted source/CSS or simulated stack rather than real browser hit-testing | A `ac07da4` browser plan; **no** until packaged execution; consume no old gate |
| #173 | old single-column gate | did not prove distinct compact List semantics/real responsive behavior | A `issue-173.red.md` plus future RTL/browser contract; consume replacement docs only |
| #178 | old cast/invented API gate | called unavailable API and encoded implementation rather than classifier authority | A matrices/`issue-178.red.md`; consume only authority map until implementation |
| #182 | `issue-182.red.test.ts`, `issue-182.red.ui.test.tsx` | copied a test-local Favorites policy and did not prove canonical inventory authority | no validated replacement yet; **do not consume** |
| #190 | any pre-`318966c` health gate | used broad #187 allowances and/or static path assertions instead of installed asset requests | `318966c`, PR211 packaged gate; consume only PR211 branch until merge |
| #191 | pre-`1e579bf` implementation-coupled selectors/guards | superseded by production FileEntry state and #187 health baseline | `1e579bf`, PR204 ordinary Bun/RTL/Playwright; consume replacement |
| #190 | old wrong-baseline health gate | treated #187 baseline or arbitrary URL as #190 acceptance | PR211 `test/e2e/plasmon-presentation-assets.spec.ts`; consume replacement |
| #65 | earlier one-file “multi-file” RTL test | one File cannot prove item transition, partial failure or duplicate suppression | repaired A `issue-65.red.ui.test.tsx`; PR208 exact-head implementation now accepted through equivalent model/adapter behavior — quarantine only this stale packet |
| #51 | earlier one-file happy-path-only UI gate | omits primitive negative/identity/collision contract | repaired lower `issue-51.red.test.ts` plus PR210 ordinary helper/RTL coverage; quarantine only this stale packet |
| #66/#182 | any packet encoding test-local visual/favorites policy | policy must come from production authority, not fixture assumptions | Coordinator must retire packet before implementation |
| #173/#175 | parse-only Playwright scripts | parse success is not execution or product RED | packaged session/CI execution only |

The registry is deliberately conservative: a packet is not promoted merely because its title sounds stronger than its assertions.
