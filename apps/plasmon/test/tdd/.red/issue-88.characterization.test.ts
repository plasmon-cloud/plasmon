// Historical #88 RED adopted into the consolidated r2 staging lane.
// The permanent production-backed guard now lives in the Shell source test;
// importing it avoids duplicating the runtime/Start/Search policy here.
import "../../../src/os/shell/runtimeOnlyInventory.test.ts";
