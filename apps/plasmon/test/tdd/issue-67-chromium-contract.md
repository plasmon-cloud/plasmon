# #67 Chromium contract

Chromium installed journey: launch Plasmon, create/open Text and Markdown,
record Worker URL/creation/handshake, exercise JS/JSON/Markdown language
operation where practical, edit/save/reopen, close each surface, and assert
strict browser health. Chromium fallback warnings are current known #67 RED,
not a pass. The test must also prove one Text A/Text B/Markdown C model does not
cross-dispose when simultaneous surfaces are supported.
