# Luna-C invalid/superseded packet registry

| packet/claim | defect | replacement |
|---|---|---|
| checkpoint statement that lane was complete | #64/#89/#113/#114/#123 and browser closures remained | Phase 2 master inventory and issue packets |
| visible Monaco editor == worker success | false browser green | #67 worker observability v2 |
| package worker HTTP 200 == Worker readiness | does not prove creation/communication | #67/#89 browser contracts |
| canvas exists == healthy js-dos/EmulatorJS | misses storage/loader/WASM/input | runtime health contracts |
| simulated fullscreen rejection == installed Photos acceptance | no Neutron policy/geometry | #180 packaged spec |
| Review first-demo UI == standalone #58 closure | different criteria/authority | #58 closure + #170 audit |
| package asset existence == native app acceptance | no semantics/focus/browser state | registry/grid/layer maps |
| #124 can proceed without save representation | screenshot must attach to accepted #64 artifact | #124 dependency envelope |
| game basename save identity | violates stable NodeId contract | #64 identity contract |

The existing `.red/issue-179` is valid staging RED and has a permanent
`text/document.test.ts` promotion destination. No old packet is deleted; these
claims must not be consumed as acceptance.
