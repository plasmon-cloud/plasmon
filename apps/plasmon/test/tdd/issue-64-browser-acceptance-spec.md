# #64 packaged browser specification

Use explicit legal js-dos fixture through normal installed FS/OpenService. Capture
an observable runtime progress change supported by the actual 8.4.1 API; invoke
only the real export/persist method; wait for completion; close the native
window; reconstruct/reopen the same game NodeId; prove restore. Then rename/move
through canonical FS and repeat. Instrument first-party requests, pageerror,
console errors, runtime readiness, save/import errors and object URL cleanup.

Run missing/corrupt/incompatible save fixtures through FS authority and assert
fresh launch remains possible. No mocked js-dos player, canvas-only readiness,
IndexedDB inspection as product persistence, or game-title dispatch. Until the
adapter exists this is adoption-ready BROWSER SPEC ONLY, with exact expected
current RED: no export/import method and no durable save record.
