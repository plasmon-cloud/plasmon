# Luna-C issue disposition summary

## Verified full RED

- **#179** — explicit deterministic `.red` gate reproduces current forced
  autosave: bytes become `after` after 15ms instead of remaining `before`, while
  the expected contract is dirty/default-off. Focused baseline session, Photos,
  and js-dos tests remain green.

## Core RED / incomplete

- **#64** save persistence seam absent; **#89** final managed Program Files /
  installed worker authority unproved; **#113/#114** requested chrome,
  minimap/commands, formatter behavior not present; **#123** artwork metadata
  contract absent.

## Browser/package spec only

- **#67** real Worker startup/communication; **#180** denied fullscreen
  installed path. #202 has a browser RED but remains blocked on implementation.

## Characterization / closure

- #48, #96, #107, #112, #121, #122, #187, Properties/Open With are audits or
  characterization, not invented REDs.

## Wait / excluded

- #124 waits for #64. #83 is Lane D. #94, #178, #181, #200 and active
  implementation/ownership fences are recorded in the inventory and not
  touched. #202 is explicitly blocked owner-unavailable.
