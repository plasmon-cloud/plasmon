# Shared dependency needs

No new shared package or build dependency is required.

The React host uses the existing `react` dependency already declared by `neutron-plasmon`. All other runtime code depends only on frozen contracts under `src/os/contracts/**`.
