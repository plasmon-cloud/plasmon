# Dependency requests

No new shared dependencies are required.

The bridge reuses the app's existing runtime dependency on `neutron-tools` for `apps.list`, `apps.describe`, `apps.install_offer`, `endpoints.list`, `workspace.open_tile`, and package-local app URL resolution. No package, lockfile, build, manifest, or Kernel changes are requested.
