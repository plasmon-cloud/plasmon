import { useEffect, useState, type CSSProperties, type ChangeEvent } from "react";
import type { AssociationRegistry, FsService, OpenTarget, ProcessController, ProcessId } from "../../os/contracts/index.ts";
import { formatBytes, settingsFeatureAvailability, summarizeStorage, type StorageSummary } from "./model.ts";
export interface SettingsDependencies { associations?: AssociationRegistry; getThemeName?: () => string; setThemeName?: (theme: string) => void; getTaskbarMode?: () => string; setTaskbarMode?: (mode: string) => void; }
export interface SettingsHostProps { processId: ProcessId; target: OpenTarget; fs: FsService; process: ProcessController; }
export function createSettingsComponent(dependencies: SettingsDependencies = {}) { return function Settings({ processId, fs, process }: SettingsHostProps) { const [storage, setStorage] = useState<StorageSummary | null>(null); useEffect(() => { process.setTitle(processId, "Settings"); void summarizeStorage(fs).then(setStorage); }, [fs, process, processId]); const theme = dependencies.getThemeName?.(); const taskbar = dependencies.getTaskbarMode?.(); return <section style={styles.root} aria-label="Settings"><h1 style={styles.heading}>Settings</h1><section style={styles.card} aria-labelledby="storage-heading"><h2 id="storage-heading" style={styles.subheading}>Storage</h2>{!storage ? <p>Calculating local storage…</p> : storage.unavailableReason ? <p role="status">Storage summary unavailable: {storage.unavailableReason}</p> : <p>{storage.files} files · {storage.directories} folders · {formatBytes(storage.bytes)} logical file data</p>}</section><section style={styles.card} aria-labelledby="appearance-heading"><h2 id="appearance-heading" style={styles.subheading}>Appearance</h2>{dependencies.setThemeName ? <label style={styles.controlRow}>Theme <select style={styles.select} value={theme ?? "system"} onChange={(event: ChangeEvent<HTMLSelectElement>) => dependencies.setThemeName?.(event.currentTarget.value)}><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></label> : <p>Theme controls will become available when Shell provides its settings callback.</p>}{dependencies.setTaskbarMode ? <label style={styles.controlRow}>Taskbar <select style={styles.select} value={taskbar ?? "default"} onChange={(event: ChangeEvent<HTMLSelectElement>) => dependencies.setTaskbarMode?.(event.currentTarget.value)}><option value="default">Default</option><option value="compact">Compact</option></select></label> : <p>Taskbar preferences will become available when Shell provides its settings callback.</p>}</section><section style={styles.card} aria-labelledby="associations-heading"><h2 id="associations-heading" style={styles.subheading}>File associations</h2><p>{dependencies.associations ? "Per-resource defaults are available through Open With. A global default-enumeration API is not exposed by the current contract." : "File association changes are available per resource through Properties / Open With."}</p></section><section style={styles.card} aria-labelledby="future-heading"><h2 id="future-heading" style={styles.subheading}>Backup & sharing</h2>{settingsFeatureAvailability.map((feature) => <p key={feature.id}><strong>{feature.label}:</strong> {feature.message}</p>)}</section></section>; }; }
export default createSettingsComponent();
const styles: Record<string, CSSProperties> = { root: { height: "100%", overflow: "auto", padding: 22, boxSizing: "border-box", background: "#171a1f", color: "#e9edf2", font: "14px/1.5 system-ui, sans-serif" }, heading: { margin: "0 0 18px", fontSize: 24, color: "#f5f7fa" }, subheading: { margin: "0 0 8px", fontSize: 16, color: "#f1f4f7" }, card: { marginBottom: 14, padding: "14px 16px", background: "#22262d", border: "1px solid #3b424d", borderRadius: 8, color: "#cbd2dc" }, controlRow: { display: "flex", alignItems: "center", justifyContent: "space-between", gap: 14, marginTop: 10, color: "#e4e9ef", fontWeight: 600 }, select: { minWidth: 150, minHeight: 32, padding: "5px 28px 5px 8px", border: "1px solid #4b5360", borderRadius: 4, background: "#12151a", color: "#f1f3f6", font: "13px/1.2 system-ui, sans-serif" } };
