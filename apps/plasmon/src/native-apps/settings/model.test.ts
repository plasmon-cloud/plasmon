import { expect, test } from "bun:test";
import type { FsService } from "../../os/contracts/index.ts";
import { createSettingsComponent } from "./Settings.tsx";
import { formatBytes, settingsFeatureAvailability, summarizeStorage } from "./model.ts";
const fs = { resolvePath: async () => ({ id: "root", parentId: null, name: "", kind: "directory", size: 0, createdAt: 0, modifiedAt: 0, metadata: {} }), stat: async (id: string) => ({ id, parentId: null, name: "", kind: "directory", size: 0, createdAt: 0, modifiedAt: 0, metadata: {} }), list: async (id: string) => id === "root" ? [{ id: "a", parentId: "root", name: "a.txt", kind: "file", size: 100, createdAt: 0, modifiedAt: 0, metadata: {} }, { id: "dir", parentId: "root", name: "dir", kind: "directory", size: 0, createdAt: 0, modifiedAt: 0, metadata: {} }] : [{ id: "b", parentId: "dir", name: "b.bin", kind: "file", size: 2048, createdAt: 0, modifiedAt: 0, metadata: {} }] } as unknown as FsService;
test("storage summary walks the existing filesystem without a new backend", async () => { expect(await summarizeStorage(fs)).toEqual({ files: 2, directories: 2, bytes: 2148 }); expect(formatBytes(2048)).toBe("2.00 KB"); });
test("Settings callback seam requires no Shell implementation import", () => { const changes: string[] = []; const Component = createSettingsComponent({ setThemeName: (value) => changes.push(value) }); expect(typeof Component).toBe("function"); expect(changes).toEqual([]); });
test("unavailable backup/sharing features are explicitly non-working", () => { expect(settingsFeatureAvailability.every((feature) => feature.available === false)).toBe(true); expect(settingsFeatureAvailability.map((feature) => feature.id)).toEqual(["backup", "sharing"]); expect(settingsFeatureAvailability.every((feature) => feature.message.includes("not integrated"))).toBe(true); });
