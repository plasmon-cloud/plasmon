import { useEffect, useState, type CSSProperties, type ChangeEvent, type FormEvent } from "react";
import type { FsService, OpenTarget, ProcessController, ProcessId } from "../../os/contracts/index.ts";
import { normalizeHttpUrl, openExternalUrl, resolveBrowserTarget } from "./url.ts";
export interface BrowserProps { processId: ProcessId; target: OpenTarget; fs: FsService; process: ProcessController; }
type BrowserState = { status: "loading"; url: string; title: string } | { status: "ready"; url: string; title: string } | { status: "error"; message: string; url: string };
export default function Browser({ processId, target, fs, process }: BrowserProps) { const [state, setState] = useState<BrowserState>({ status: "loading", url: target.url ?? "", title: "Browser" }); const [address, setAddress] = useState(target.url ?? ""); useEffect(() => { let active = true; setState({ status: "loading", url: target.url ?? "", title: "Browser" }); void resolveBrowserTarget(target, fs).then((location) => { if (!active) return; setAddress(location.url); setState({ status: "loading", ...location }); process.setTitle(processId, location.title || "Browser"); }).catch((error: unknown) => { if (!active) return; setState({ status: "error", message: error instanceof Error ? error.message : String(error), url: target.url ?? "" }); process.setTitle(processId, "Browser"); }); return () => { active = false; }; }, [fs, process, processId, target.nodeId, target.url]); const navigate = (event: FormEvent) => { event.preventDefault(); const normalized = normalizeHttpUrl(address); if (!normalized) { setState({ status: "error", message: "Enter a complete http:// or https:// URL", url: address }); return; } process.setTarget(processId, { url: normalized }); }; const external = () => { const value = state.url || address; if (typeof globalThis.window !== "undefined") openExternalUrl(value, globalThis.window.open.bind(globalThis.window)); }; const canOpen = Boolean(normalizeHttpUrl(state.url || address)); return <section style={styles.root} aria-label="Web browser"><style>{`.plasmon-browser-address::placeholder { color: #858e9b; opacity: 1; }`}</style><form style={styles.toolbar} onSubmit={navigate}><label htmlFor={`browser-address-${processId}`} style={styles.srOnly}>Web address</label><input id={`browser-address-${processId}`} className="plasmon-browser-address" value={address} onChange={(event: ChangeEvent<HTMLInputElement>) => setAddress(event.currentTarget.value)} style={styles.address} inputMode="url" autoCapitalize="none" autoCorrect="off" spellCheck={false} placeholder="https://example.com"/><button type="submit" style={buttonStyle(false)}>Go</button><button type="button" style={buttonStyle(!canOpen)} onClick={external} disabled={!canOpen}>Open externally</button></form><div style={styles.notice}>Some sites block embedded browsing. Use “Open externally” when a page refuses to load here.</div>{state.status === "error" ? <div style={styles.error} role="alert">{state.message}</div> : state.url ? <div style={styles.frameWrap}>{state.status === "loading" && <div style={styles.loading}>Loading…</div>}<iframe key={state.url} title={state.title || "Website"} src={state.url} style={styles.frame} sandbox="allow-forms allow-popups allow-popups-to-escape-sandbox allow-scripts" referrerPolicy="no-referrer" onLoad={() => setState((current) => current.status === "error" ? current : { ...current, status: "ready" })} onError={() => setState({ status: "error", message: "This site could not be embedded. Open it externally instead.", url: state.url })}/></div> : <div style={styles.empty}>No web address is available.</div>}</section>; }
function buttonStyle(disabled: boolean): CSSProperties { return { minHeight: 32, padding: "5px 11px", border: "1px solid #4a515d", borderRadius: 4, background: disabled ? "#252a31" : "#343a43", color: disabled ? "#7f8792" : "#f1f3f6", font: "600 12px/1.2 system-ui, sans-serif", cursor: disabled ? "not-allowed" : "pointer", opacity: 1 }; }
const styles: Record<string, CSSProperties> = { root: { display: "flex", flexDirection: "column", height: "100%", minHeight: 0, background: "#171a1f", color: "#edf1f5" }, toolbar: { display: "flex", gap: 8, alignItems: "center", padding: 8, background: "#20242a", borderBottom: "1px solid #3b424d" }, address: { flex: 1, minWidth: 0, minHeight: 32, boxSizing: "border-box", padding: "6px 9px", border: "1px solid #4a515d", borderRadius: 4, outline: 0, background: "#111419", color: "#f1f3f6", caretColor: "#f1f3f6", font: "13px/1.3 ui-monospace, monospace" }, notice: { padding: "6px 10px", fontSize: 12, color: "#aeb6c1", background: "#1b1f25", borderBottom: "1px solid #343b45" }, frameWrap: { position: "relative", flex: 1, minHeight: 0, background: "#fff" }, frame: { width: "100%", height: "100%", border: 0, background: "#fff" }, loading: { position: "absolute", inset: 0, display: "grid", placeItems: "center", zIndex: 1, pointerEvents: "none", color: "#d8dde4", background: "rgba(23,26,31,.86)" }, error: { display: "grid", placeItems: "center", flex: 1, padding: 24, color: "#ffd6da", background: "#29171a", textAlign: "center" }, empty: { display: "grid", placeItems: "center", flex: 1, padding: 24, color: "#aeb6c1" }, srOnly: { position: "absolute", width: 1, height: 1, padding: 0, margin: -1, overflow: "hidden", clip: "rect(0,0,0,0)", whiteSpace: "nowrap", border: 0 } };
