# Native Browser

Opens direct HTTP(S) `OpenTarget.url` values or existing Wave 1 `.url` shortcuts. Unsafe schemes are rejected. Embedding is best-effort in a sandboxed iframe and every valid target can be opened externally with `_blank`, `noopener`, and `noreferrer`. This app contains no Neutron bridge/app-frame handling.
