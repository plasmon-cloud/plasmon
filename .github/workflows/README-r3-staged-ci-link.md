# Staged CI note

The R3 staged review/merge-queue/post-merge CI contract is documented in [`PLASMON_STAGED_CI.md`](./PLASMON_STAGED_CI.md). This small companion file exists only as a discoverable workflow-directory link until the canonical workflow README is reconciled in the same implementation PR.
