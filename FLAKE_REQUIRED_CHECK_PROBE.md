# Required Flake Summary Probe

Temporary CI diagnostic file.

Purpose: verify whether a pull request that does not match `.github/workflows/plasmon-flake-probe.yml` path filters becomes blocked when `Flake probe summary` is configured as a required check.

This file intentionally does not touch Plasmon tests, test harnesses, Playwright configuration, package manifests, Nix files, or the Flake Probe workflow itself.
